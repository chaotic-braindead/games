rad = math.atan2(my - y, mx - x)
            # power = math.hypot(my - y, mx - x) * 0.05
            # dist = math.hypot(my - y, mx - x) // power
            # dx = math.cos(rad) * power
            # dy = math.sin(rad) * power